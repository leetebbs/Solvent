# Solvent - An auto liquidator for Aave protocols

## Install Libraries

Install `chainlink` & `openzeppelin` contracts:

```
forge install openzeppelin/openzeppelin-contracts smartcontractkit/foundry-chainlink-toolkit --no-commit
```
